name = "drcenik-redirects"
main = "worker.js"
compatibility_date = "2024-06-01"

# Nach Cloudflare-Zone-Import anpassen und deployen:
#   cd infra/cloudflare && npx wrangler deploy
#
# Alternative ohne Worker: Redirect Rules aus bulk-redirects.csv + README.md

[[routes]]
pattern = "www.drcenik.at/*"
zone_name = "drcenik.at"

[[routes]]
pattern = "drcenik.at/*"
zone_name = "drcenik.at"
